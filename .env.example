# Local secrets for script/refresh-next-event-dates.rb
# Copy to `.env` (which is gitignored) and fill in the URL.
# The Ruby script loads this file automatically at startup.

# Healthchecks.io ping URL — dead man's switch + failure routing.
# 1. Create a free account at https://healthchecks.io
# 2. Add a Check with period = 1 day, grace = 1 day
# 3. Paste the ping URL here (looks like https://hc-ping.com/UUID)
# 4. In the check's Integrations tab, connect Telegram (or email/Slack/etc).
#    In the integration settings, ensure "Notify when check goes down" is ON
#    and "Notify when check comes back up" is OFF — otherwise you get noisy
#    success messages alongside the real alerts.
HEALTHCHECKS_URL=

# script/ga-report.ts (daily show traffic reports from Google Analytics).
# GA_REPORTS_CREDENTIALS: path to a service-account key JSON (relative to the repo
# root) whose email is a Viewer on the GA4 property. Leave empty to fall back to
# gcloud Application Default Credentials (fine by hand, expires under cron).
GA_REPORTS_CREDENTIALS=ga-reports-sa.json
# Separate Healthchecks check for the reports job (period 1 day, grace 1 day).
GA_REPORTS_HEALTHCHECKS_URL=

# Meta ads scripts (META_ADS.md phase 1 and 7). META_ACCESS_TOKEN is a system-user token
# generated in Business settings > Users > System users with ads_management, ads_read,
# business_management, pages_read_engagement. Ids and budgets live in meta-ads/config.yml.
META_ACCESS_TOKEN=
# Healthchecks check shared by the meta-*.ts jobs (period 1 day, grace 1 day).
META_ADS_HEALTHCHECKS_URL=

# ROBINs staff calendar (script/robins-calendar.ts): the exact title of the calendar in Calendar.app
ROBINS_CALENDAR="Test Calendar"
